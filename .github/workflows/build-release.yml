name: Build and Release Windows App

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Executable with PyInstaller
        run: |
          pyinstaller --noconfirm --onedir --windowed --name "libras-to-speech" --collect-all mediapipe --collect-all sklearn --collect-data spellchecker --hidden-import pyttsx3.drivers --hidden-import pyttsx3.drivers.sapi5 main.py

      - name: Zip the build
        run: |
          Compress-Archive -Path dist/libras-to-speech -DestinationPath libras-to-speech-windows.zip

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v2
        with:
          files: libras-to-speech-windows.zip
          generate_release_notes: true
